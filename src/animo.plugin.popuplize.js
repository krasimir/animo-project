(function() {

    if(typeof animo == "undefined") {
        throw new Error("Missing animo.js!");
        return;
    }

    if(typeof animo.plugins == "undefined") {
        animo.plugins = {};
    }

    animo.plugins.popuplize = function() {
        
    }

})();